# render-markdown backlog

## Planned

- [ ] **Print-quality CSS** — Add `@media print` styles to the rendered HTML template. Hide the TOC sidebar, theme selector, and source footer. Optimize typography for paper: serif body font, tighter line-height, proper page breaks (`break-inside: avoid` on code blocks, tables, headings). Test with Cmd+P across themes.
