# render-markdown — Backlog

## Planned Improvements

- [ ] **Syntax highlighting** — code blocks render as plain `<pre>`; add highlight.js or Shiki for colored code
- [ ] **Live reload** — watch the `.md` file and auto-refresh the browser tab on save
- [ ] **Print/export to PDF** — `--pdf` flag using Puppeteer or WeasyPrint
- [ ] **Mermaid diagrams** — render fenced `mermaid` blocks inline
- [ ] **Custom CSS override** — `config.json` key for an extra CSS file layered on top of the theme
- [ ] **Dark/light toggle** — inject a button in the rendered page to flip themes without re-running
- [ ] **Responsive no-sidebar mode** — collapse TOC on narrow windows / add `--no-toc` flag
